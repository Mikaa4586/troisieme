class MusicPlayer
{
    constructor(format)
    {
        this.format = format;
        document.write(format);
    }
}
let music = new MusicPlayer("Nvidia <br>");
let music3 = new MusicPlayer("LOl");